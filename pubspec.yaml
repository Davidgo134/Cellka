name: cellka
description: "Cellka — карта вышек, запись и анализ мобильной сети"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^7.0.0
  flutter_map_marker_cluster: ^8.2.2
  latlong2: ^0.9.1
  geolocator: ^12.0.0
  permission_handler: ^11.4.0
  sqflite: ^2.4.3
  path_provider: ^2.1.6
  path: ^1.9.1
  uuid: ^4.6.0
  equatable: ^2.1.0
  provider: ^6.1.5+1
  intl: ^0.19.0
  share_plus: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
