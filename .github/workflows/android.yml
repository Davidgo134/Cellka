name: Android CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  analyze-and-test:
    name: Analyze & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - run: flutter analyze --no-fatal-infos --no-fatal-warnings
      - run: flutter test

  build-debug-apk:
    name: Build debug APK
    needs: analyze-and-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Java 21: нативная библиотека Yandex MapKit собрана под class file 65 (Java 21)
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      # Догенерирует недостающие бинарные файлы (gradle wrapper jar, иконки),
      # не перезаписывая запушенные вручную (AndroidManifest, MainActivity, gradle-скрипты).
      - name: Restore missing generated files
        run: flutter create --platforms=android --project-name cellka --org com.github.davidgo134 .
      # Убираем сгенерированные .kts-скрипты — в проекте используются Groovy .gradle
      - name: Remove conflicting generated KTS scripts
        run: rm -f android/settings.gradle.kts android/build.gradle.kts android/app/build.gradle.kts
      - run: flutter pub get
      - name: Build debug APK
        env:
          MAPKIT_API_KEY: ${{ secrets.YANDEX_MAPKIT_API_KEY }}
        run: flutter build apk --debug --dart-define=OPENCELLID_API_KEY=${{ secrets.OPENCELLID_API_KEY }}
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: cellka-debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 14
